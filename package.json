{
  "name": "consumer-app",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,client -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:api": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "db:migrate": "cd server && npx prisma migrate dev",
    "db:seed": "cd server && npx tsx prisma/seed.ts",
    "db:reset": "cd server && npx prisma migrate reset --force"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
